# Uncomment this line to define a global platform for your project
# platform :ios, "6.0"

inhibit_all_warnings!

target "Cloudmine Learning Trail iOS" do
pod "CloudMine"
end

target "Cloudmine Learning Trail iOSTests" do

end
